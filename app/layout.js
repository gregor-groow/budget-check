export const metadata = {
  title: 'groow · Budget Check',
  description: 'Google Ads Budget Monitor',
};

export default function RootLayout({ children }) {
  return (
    <html lang="sk">
      <body style={{ margin: 0, padding: 0 }}>{children}</body>
    </html>
  );
}
